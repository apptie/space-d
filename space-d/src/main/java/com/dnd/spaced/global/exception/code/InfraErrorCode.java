package com.dnd.spaced.global.exception.code;

public enum InfraErrorCode implements ErrorCode {

    NOT_FOUND_AVAILABLE_PORT,
    CANNOT_EXECUTE_EMBEDDED_REDIS,
    INVALID_BLACKLIST_TOKEN_CONTENT
}
